name: Auto Update Episodes

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install requests beautifulsoup4 lxml

      - name: Run fetch script
        run: python fetch_episodes_ready.py

      - name: Install jq
        run: sudo apt-get update && sudo apt-get install -y jq

      - name: Update episodes.json via GitHub API
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          content=$(base64 -w 0 episodes.json)
          data=$(jq -nc --arg msg "🔄 تحديث حلقات" --arg content "$content" '{
            message: $msg,
            content: $content,
            branch: "main"
          }')
          curl -X PUT \
            -H "Authorization: token $GITHUB_TOKEN" \
            -H "Content-Type: application/json" \
            -d "$data" \
            "https://api.github.com/repos/${{ github.repository }}/contents/episodes.json"
